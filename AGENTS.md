# 🤖 MiniGame — 项目智能体事实源与协作规范 (AGENTS.md)

本项目为基于现代 Web 3D (Three.js ESM) 与生成式 AI 资产构建的网页小游戏合集。多智能体协作与后续迭代应以此文档为最高事实依据。

---

## 🕹️ 游戏矩阵与架构事实

### 1. 🐍 3D 贪吃蛇 (`games/snake3d/`)
- **视觉风格**：Q 版任天堂/动森风「3D 清新草地积木乐园」；
- **环境渲染**：
  - 3D 粘土风全景天幕背景（DOM 容器 CSS Cover + Three.js Alpha 穿透，确保移动端竖屏与桌面端全屏 0 畸变防拉伸）；
  - 立体多层浮空岛地基（草皮层、泥土层、倒锥岩体底座、四角发光灯塔立柱）；
  - 棋盘草坪微抬升至 $y=0.02$ 以彻底杜绝与地台顶层的 Z-Fighting；
- **蛇体形态**：
  - 萌系头颅（大眼、腮红、动态吐信小红舌）；
  - 仿生变径锥度收束（Tapering：头部 $1.15$、颈部 $0.98$、躯干 $0.95$、尾部收束至 $0.30$ 配专属尾尖）；
  - 脊椎切线旋转自适应弯曲 + S 游动微幅横向正弦波浪；
  - 新增身体段采用 `Spring Ease` 弹性生长动画（0.05 膨胀至 1.0），严禁突兀硬蹦；
- **动力学**：恒速线性插值 + 亚帧时间余量累加器（`this.moveTimer -= this.currentInterval`），严禁使用两端导数为 0 的 `smoothstep`；
- **交互与 UI**：
  - 3D 粘土质感卡片弹窗 + 3D 泡泡字标头徽章；
  - 高对比度独立胶囊药丸（3D 爆炸图标配独立深红粉底标签）；
  - 物理按压玩具按钮（`toy-btn`）；
  - 移动端 360° 弹性虚拟摇杆 + 桌面方向键/WASD。

### 2. 🚀 太空战机 (`games/skydodge/`)
- **视觉风格**：次世代深空街机；
- **3D 资产**：经 Meshy.ai 生成并由 `scripts/optimize-assets.mjs` 完成 90%+ 瘦身与 Meshopt 二进制压缩的 4 款核心模型（超音速战机、晶脉深空陨石、等离子护盾球、量子能量核）；
- **玩法机制**：机载等离子双激光主炮循环对象池、陨石爆破粒子、飞行动力学侧倾 (Banking Roll & Pitch)；
- **全端操控**：左拇指虚拟摇杆 + 右拇指开火与冲刺按键。

### 3. 🌻 花园守卫 (`games/gardenguard/`)
- **视觉风格**：Q 版粘土 / Low-Poly Claymation「立体浮空岛花园」；
- **环境与场景**：
  - 45° 等距正交俯视机位（移动端自适应视锥缩放，防拉伸防出界）；
  - 3D 粘土风全景天幕背景（DOM 容器 CSS Cover + Three.js Alpha 穿透）；
  - 多层浮空岛台地基（暖草绿草皮、焦糖泥土断层、倒锥岩底、外圈木栅栏与微动花朵）；
  - 4×4 格子阵列居中，外围围绕式暖沙碎石怪物进攻道路；
- **战斗与合成系统**：
  - 纯 TS/JS 三层解耦（Core / Renderer / UI）；
  - 2048 经典两同合一（1~5 星进化），5 星解锁全屏终极打击技能；
  - 射击 / 群伤 / 减速 / 爆发 4 系植物炮台与 6 种害虫及精英 Boss 无尽波次对抗；
  - 花园核心生命守护（受击变色与树冠震颤动画）；
- **全端交互**：桌面鼠标拖拽与移动端单指流畅触摸合成。

---

## 🚫 项目全局研发四大铁律（硬性红线与自动门禁）

所有小游戏在交付与上线前必须通过以下四大铁律检查，违反任何一条均属于验收不合格：

1. **纯中文零英文铁律**：
   - 中文版本游戏界面中的所有用户可见区域（HUD 顶底栏、卡片标题、按钮文本、技能指南、弹窗说明、调试工具箱），**严禁出现任何英文单词或缩写**（如 `HP` 必须改为 `生命`、`Wave` 必须改为 `波次`、`Lv` 必须改为 `等级`、`MAX` 必须改为 `已满级`、`R/SR` 必须改为 `稀有/卓越`）；
   - 游戏标题副标题严禁放置多余英文字母，保证纯正中文阅读体验。
2. **零系统原生 Emoji 铁律**：
   - 严禁在 HTML 结构、CSS 内容（`content: ""`）或 JS 动态模板字符串中书写任何系统原生 Unicode Emoji（如 ❤️, 🌊, 💎, 🌱, ⭐, 🪙, 🏆, 🏡, 📖, ⏸️, 💀, 🎯, 💥, ❄️, 💣 等）；
   - 所有图标必须统一使用 `assets/icons/` 原生生成的透明 3D 游戏质感图标（通过 `<img>` 标签加载）。
3. **实体化粘土建模铁律（严禁粗糙几何体冒充角色）**：
   - 严禁用裸露单一的 Cone、Cube 或 Sphere 充当游戏植物、炮台或怪物；
   - 植物必须具备参数化手办构件：**立体陶土花盆底座 + 弯曲叶茎 + 饱满果冻头颅 + 喇叭喷筒嘴 + 3D 立体眼睛腮红**；
   - 害虫怪物必须具备实体特征：**多节蠕动身体 + 触角 + 眼睛 + 步足**；
   - 场景地台必须拥有：**焦糖泥土断层、倒锥岩底、双层草皮、石砖小道、花草微风车**。
4. **全端 UI 零溢出与防遮挡铁律**：
   - 严禁任何悬浮按钮、选项菜单、弹窗卡片在桌面端或移动端超出屏幕视口（`rect.right <= window.innerWidth`）；
   - 快捷评测与设置面板必须做自适应贴边或安全区内部展开，杜绝任何边缘截断与文字看不全。

---

## 🛠️ 研发与迭代必须遵循的核心准则

1. **防抖与防晕原则**：
   - 严禁相机贴身急动追踪物体转弯；
   - 严禁在高频事件（如吃食物、得分）中破坏性修改相机 FOV。
2. **防卡顿原则**：
   - 连续网格运动必须使用恒定速度位移插值；
   - 步进累加器必须保留时间余量，严禁直接赋 0。
3. **防 Z-Fighting 原则**：
   - 严禁不同 Mesh 的面处于相同物理 Y 坐标，重叠面间距必须 $\ge 0.02$；
   - 平铺纹理设置 `ClampToEdgeWrapping`，禁用冗余 mipmaps。
4. **UI 可读性与 3D 图标规范**：
   - 严禁白底白字或低对比浅灰字；失败标签、警告状态必须使用独立高对比度徽章。

---

## 🧪 自动化测试套件

修改任何游戏源码后，必须运行对应的无头 Chrome 自动化测试套件进行回归验证：

```bash
# 3D 贪吃蛇测试
node scripts/test-gameplay.mjs          # 验证桌面端开始卡片、3D浮空岛、蛇体变径与GameOver弹窗
node scripts/test-mobile.mjs            # 验证移动端视口 (390×844) 360°虚拟摇杆与触控

# 太空战机测试
node scripts/test-skydodge.mjs          # 验证激光发射、陨石碰撞爆炸与HUD
node scripts/test-skydodge-mobile.mjs   # 验证双拇指虚拟触控

# 花园守卫测试
node scripts/test-gardenguard.mjs        # 验证桌面端战斗、合成、波次与HUD
node scripts/test-gardenguard-mobile.mjs # 验证移动端视口 (390×844) 与触摸拖拽
```

---

## 📚 详细专项文档索引

- [EXPERIENCE_AND_CHECKLIST.md](file:///Users/hunkwu/Desktop/ai/minigame/docs/EXPERIENCE_AND_CHECKLIST.md) — 3D 游戏体验调优避坑指南与上线核查清单
