# 📝 项目更新日志 (CHANGELOG)

所有对 MiniGame 项目的关键功能迭代、架构调整与体验修复均记录于此。

---

## [2026-09-20] — 全维度 3D 视觉重塑、动力学抗卡顿与双游戏体验升级

### 🚀 所做更新 (Updates)

#### 1. 🐍 3D 贪吃蛇 (Snake 3D)
- **原生生图 3D 素材定制与集成**：
  - 利用原生生图能力设计 3D 粘土风全景天幕背景（`snake3d_sky_bg.jpg`），融合柔和体积云、暖金阳光光束与远景微距散景；
  - 设计并提取透明通道的 3D 泡泡字标头徽章（`snake3d_gameover_badge.png`）与萌系小绿蛇标题徽章（`snake3d_start_badge.png`）。
- **动力学抗卡顿与丝滑平滑化**：
  - 彻底废除网格步进中的 `smoothstep` 插值，改为**恒速线性位移插值**，杜绝每格起止点速度归零带来的机械刹停顿挫；
  - 引入**亚帧时间余量累加器 (Sub-frame Accumulator)**，步进保留余量时间（`moveTimer -= currentInterval`），彻底解决 60/120Hz 下丢帧抖动。
- **拟真蛇体骨骼与形态变径 (Tapering)**：
  - 重塑为圆润互锁球形/胶囊身体段；
  - 引入仿生变径锥度收束：头部饱满 ($1.15$) $\rightarrow$ 颈部收束 ($0.98$) $\rightarrow$ 躯干匀称 ($0.95$) $\rightarrow$ 尾部向后逐节收拢至 $0.30$，并在末节配置专属锥形尾尖（Tail Tip）；
  - 增加**脊椎切线平滑旋转 (Tangent Alignment)** 与 **S 游动正弦横摆波浪**；
  - 新增节段采用 `Spring Ease` 弹性生长动画（0.05 绽放至 1.0），消除方块生硬瞬间蹦出的突兀感。
- **立体多层浮空岛地台构建**：
  - 构建草皮层（-0.22）、焦糖泥土层（-0.9）、倒锥悬空岩体底座（-1.9 ~ -2.8）与四角立体发光灯塔立柱；
  - 棋盘草坪微抬升至 $y=0.02$ 且锁定 `ClampToEdgeWrapping`，彻底杜绝 Z-Fighting 频闪条纹。
- **次世代 3D 粘土风 UI 与高对比度弹窗**：
  - 弹窗升级为加厚立体奶白圆角卡片（Claymorphism Card）+ 3D 泡泡字标头；
  - 失败原因采用高对比度独立珊瑚红药丸徽章（`💥 撞到墙壁`），彻底解决原白底白字隐形失真问题；
  - 得分面板升级为烫金立体数字与金色立体浮雕凹槽；
  - 交互按钮升级为具有物理下陷位移的立体玩具按钮（`toy-btn`）。
- **360° 弹性虚拟摇杆与 Q 版互动音效体系**：
  - 移除生硬 4 方向 D-Pad，升级为现代移动端主流的 360° 弹性虚拟摇杆（38px 限制位移、弹性回中与 4 扇区智能角度映射）；
  - Web Audio 增补清脆苹果咬嚼音、卡通气泡转向音、风铃仙女音、碎石低音轰响与升级乐段。

#### 2. 🚀 太空战机 (SkyDodge 3D)
- **Meshy.ai 3D 资产管线与瘦身**：
  - 超音速战机、晶脉深空陨石、等离子护盾球、量子能量核 4 款核心模型，通过 `scripts/optimize-assets.mjs` 实现 90%+ 极致瘦身与 Meshopt 拓扑压缩；
- **武器系统与动力学**：
  - 40 发循环等离子激光主炮对象池，激光命中陨石触发多碎片粒子爆炸与连击加成；
  - 飞行动力学侧倾（最大 37° Banking Roll 与俯仰 Pitch）；
  - 双喷口蓝紫离子尾焰动态抖动与全套 Web Audio 太空音效；
  - 移动端双拇指触控体系（左手摇杆 + 右手开火/加速按键）。
- **移动端性能调优与分级管线 (Mobile Performance Pipeline)**：
  - 废除 `preserveDrawingBuffer: true`（重置为 `false`），消除移动端 GPU 每帧强制读回拷贝的 20%~30% 性能负担；
  - 移动端 DPR 限制在 `Math.min(devicePixelRatio, 1.5)`，避免 Retina 屏幕像素超载；
  - `UnrealBloomPass` 移动端启用 `0.5x` 降采样分辨率，后处理模糊卷积运算提速 4 倍，且光晕更柔和自然；
  - 移动端关闭深空场景冗余的 `shadowMap` 阴影渲染，成倍减少 Draw Calls。
- **战机深空暗光重塑与立体采光 (Illumination & PBR Remaster)**：
  - 打造战机专属**三点式跟随照明系统**（机背主高光、侧前翼缘天青轮廓光、机腹暖金补光），采用精确衰减半径点光源，机身棱角与流线纤毫毕现；
  - 增设天顶冷青 (`0x38BDF8`) 与底部暖紫 (`0x8B5CF6`) 的深空双色半球光，全视角驱散死黑；
  - 战机 PBR 材质自适应增益：钳制 `metalness <= 0.38`，平衡粗糙度并在 HSL 空间柔和提亮底色，彻底杜绝无环境贴图时的镜面吸光剪影；
  - 赛博地峡网格改用 `NormalBlending`，星空微粒设为 `0.35` 并在接近战机前方（`playerZ - 2`）即时回收，彻底根治远景灭点白爆与近景光球贴脸穿帮。

---

### ⚠️ 遇到问题及解决方案 (Issues & Solutions)

| 序号 | 遇到的问题与现象 | 根本原因剖析 | 实施解决方案 |
| :--- | :--- | :--- | :--- |
| 1 | **贪吃蛇移动过程中存在严重的周期性顿挫与微卡顿** | 原代码在网格步进中采用了 `smoothstep(rawT)` 缓动函数，由于两端导数均为 0，导致蛇在每个格子起步和结束时速度强制降为 0（每秒刹车顿挫约 6 次）；同时计时器在 step 触发时直接抹零，丢弃了帧间多余时间。 | 彻底废止 `smoothstep`，改为**恒定速度位移线性插值**；采用**亚帧累加器**保留时间余量（`this.moveTimer -= this.currentInterval`），跨网格运动顺滑如丝。 |
| 2 | **GameOver 弹窗视觉简陋，失败原因白底白字失真隐形** | 原弹窗为单薄白色方块加纯色文字，`撞到墙壁` 为半透明白字直接放在浅色卡片上，对比度极低，严重不可读；标头使用粗糙的红色模糊外发光，缺乏游戏质感。 | 重塑为 **3D 粘土质感卡片 (Claymorphism Card)**，加入内高光倒角与厚度下沉阴影；失败原因采用独立高对比度珊瑚红胶囊药丸（`💥 撞到墙壁`，深红文字）；得分采用烫金立体浮雕凹槽；按钮引入带物理按压位移的 3D 玩具按钮（`toy-btn`）。 |
| 3 | **蛇身方块化，吃食物尾部瞬间生硬蹦出，无蛇类仿生形态** | 身体段采用未旋转的离散 `BoxGeometry`，没有骨骼切线旋转，没有渐细收束，吃食物时新段在末尾以 100% 尺寸突兀出现。 | 重构为圆润互锁球形段，设计**仿生变径锥度曲线**（头部 1.15 $\rightarrow$ 颈部 0.98 $\rightarrow$ 躯干 0.95 $\rightarrow$ 尾部收束至 0.30 搭配专属尾尖）；引入**脊椎切线平滑旋转**与 **S 游动横向正弦波**；新增身体段采用 `Spring Ease` 弹性生长动画（0.05 膨胀至 1.0）。 |
| 4 | **棋盘草坪出现奇怪的水平横向条纹与频闪噪波** | 地面网格 `PlaneGeometry` 与浮空岛顶层草皮 `grassCrust` 均位于相同坐标 $y=0$，发生 GPU 深度缓冲竞争（Z-Fighting）；且纹理过滤未限制边缘包裹。 | 将棋盘草坪微升至 $y=0.02$，浮空岛草皮顶面调至 $-0.02$，拉开物理标高安全差；纹理开启 `ClampToEdgeWrapping` 并设置 `LinearFilter`，彻底消除条纹噪波。 |
| 5 | **视角晃动眩晕，吃食物画面不合时宜地拉伸缩放** | 摄像机每帧贴身追赶转弯物体的坐标，导致剧烈急动甩头；同时进食逻辑频繁调用 `punch(52, 0.2)` 破坏性压缩 FOV。 | 相机提高至开阔稳定的全景机位（$Y=28.5, Z=19.5, \text{FOV}=46^\circ$），仅保留 $< 0.12$ 的柔和呼吸微跟随；彻底注销破坏性 FOV punch，全景稳定开阔。 |
| 6 | **太空战机在手机浏览器运行卡顿且掉帧** | `preserveDrawingBuffer: true` 强制每帧全量读回显存；Retina 屏 3x 原生 DPR 下运行全屏多级高斯模糊 UnrealBloomPass，GPU fill-rate 严重超载；无地表太空依然开启实时阴影。 | 重置为 `preserveDrawingBuffer: false`；移动端限制 DPR $\le 1.5$；后处理采用 0.5x 降采样模糊；移动端关闭阴影渲染，手机端稳稳锁定 60fps。 |
| 7 | **深空战机过暗变成死黑剪影，看不清流线机身与细节** | Meshy.ai 生成的模型在无 HDR 天空盒反射时，过高的 `metalness` 像黑镜一样吸光；场景缺少全方位漫反射与战机局部轮廓高光。 | 打造战机跟随三点式点光源（背光+轮廓光+补光）；场景增设天顶冷青+底部暖紫双色半球光；PBR 材质钳制金属度 $\le 0.38$ 并提升底色漫反射，战机机械立体感全面呈现。 |
| 8 | **地峡网格远景灭点刺眼白爆，星辰微粒贴脸遮挡屏幕** | 航道地面网格使用 `AdditiveBlending`，远景几十条透视线在灭点叠加引发白爆；星辰微粒尺寸达 1.05 且生命周期贯穿相机后方，在距离镜头 0.2m 处被放大成巨大白色圆盘遮挡视线。 | 地峡网格改用 `NormalBlending`，彻底消除能量叠加白爆；星辰微粒尺寸降为 `0.35` 并在到达战机前方（`playerZ - 2`）即时回收，深空通透且永不遮挡战机。 |

---

### 🎯 明日计划 (Tomorrow's Plan)

1. **关卡深度与特色道具扩展**：
   - 贪吃蛇新增磁力吸附果实（Magnet Fruit）、冰冻减速道具（Freeze Slowmo）与彩虹连线加分机制；
   - 太空战机新增第二波次精英陨石群与周期性母舰 Boss 战斗。
2. **战绩系统与本地持久化存储**：
   - 接入本地 LocalStorage，记录最佳得分、最高连击、游玩时长与新纪录动画弹窗；
   - 设计 3D 奖牌与结算分享卡片。
3. **加载优化与首屏体验**：
   - 增加 Web Audio 首帧静音解锁引导与加载过渡骨架屏；
   - 针对低配设备开启自动降级策略（阴影分辨率自适应与像素比动态调节）。
